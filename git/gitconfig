[alias]
	sstatus = status -sb
	llog = log --oneline -8
	last = log -1 HEAD --stat
	new = checkout -b
	amend = commit --amend --no-edit
	checklast = checkout -
	unstage = reset HEAD --
	diffstaged = diff --cached
	remove = resotre --staged
